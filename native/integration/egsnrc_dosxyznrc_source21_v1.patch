diff --git a/Makefile b/Makefile
index dd53271..f976d61 100644
--- a/Makefile
+++ b/Makefile
@@ -52,8 +52,15 @@ RANDOM = $(EGS_SOURCEDIR)ranmar
 
 USER_CODE = dosxyznrc
 
+MCDOSE_PARTICLE_DMLC_BUILD ?=
+MCDOSE_PARTICLE_DMLC_SOURCE_BRIDGE = $(MCDOSE_PARTICLE_DMLC_BUILD)/libmcdose_particle_dmlc_egsnrc_source_mortran_bridge.a
+
 EGS_EXTRA_OBJECTS = $(CUTIL_OBJECTS) $(PARDOSE_OBJECTS) $(BEAMLIB_OBJECTS) $(BEAM_OBJECTS) $(VCULIB_OBJECTS)
-EGS_EXTRA_LIBS = $(FLIBS) $(BEAMLIB_EXTRA_LIBS)
+EGS_EXTRA_LIBS = $(FLIBS) $(BEAMLIB_EXTRA_LIBS) \
+                 $(MCDOSE_PARTICLE_DMLC_SOURCE_BRIDGE) \
+                 -L$(MCDOSE_PARTICLE_DMLC_BUILD) \
+                 -Wl,-rpath,$(MCDOSE_PARTICLE_DMLC_BUILD) \
+                 -lmcdose_particle_dmlc_egsnrc_source -lstdc++
 
 SOURCES = $(EGS_SOURCEDIR)egsnrc.macros \
           $(EGS_UTILS)timing.macros \
@@ -62,6 +69,7 @@ SOURCES = $(EGS_SOURCEDIR)egsnrc.macros \
           $(EGS_SOURCEDIR)transportp.macros \
           $(EGS_SOURCEDIR)pegs4_macros.mortran \
           $(EGS_UTILS)phsp_macros.mortran $(IAEA_PHSP_MACROS)\
+          mcdose_particle_dmlc_source_v1.mortran \
           $(USER_CODE).mortran \
           $(EGS_SOURCEDIR)get_inputs.mortran \
           $(EGS_SOURCEDIR)get_media_inputs.mortran $(MACHINE_MORTRAN) \
diff --git a/dosxyznrc.mortran b/dosxyznrc.mortran
index 7ec5093..59bf6b4 100644
--- a/dosxyznrc.mortran
+++ b/dosxyznrc.mortran
@@ -1424,6 +1424,8 @@ external combine_results;
 
 character medChar($IMAX);
 character encoding*62;
+character phantomMediaRecord*1536;
+$LOGICAL integerMediumFormat;
 
 #ifdef HAVE_C_COMPILER;
 $REAL   part_dose, part2_dose, current_result, current_uncertainty;
@@ -1551,6 +1553,12 @@ IF(NMED = 0)[ "Input data from CT data file as processed by the code ctcreate"
     "read number of media"
     read($CTUnitNumber,'(i2)')NMED;
     OUTPUT61 NMED;(/' NMED = ',i2);
+    IF(NMED < 1 | NMED > $MXMED)[
+       OUTPUT61 NMED,$MXMED;
+       (/' ***ERROR: CT phantom medium count ',i5,
+         ' is outside the compiled range 1..',i5/);
+       STOP;
+    ]
     "media names"
     OUTPUT61;(/' media:');
     DO n=1,NMED[
@@ -1591,19 +1599,66 @@ IF(NMED = 0)[ "Input data from CT data file as processed by the code ctcreate"
       ' y range : ',F12.5,' - ',F12.5,' cm'/
       ' z range : ',F12.5,' - ',F12.5,' cm');
 
-    "input the media numbers"
+    "input the media numbers.  Standard EGSnrc uses one base-62 character"
+    "per voxel; legacy UVA phantoms use one fixed-width I3 integer."
     DO k=1,KMAX[
        DO j=1,JMAX[
-          "Read in the characters that correspond to medium numbers"
-          read($CTUnitNumber,:medformat:) (medChar(i),i=1,IMAX);
-          "Convert from ASCII to a number"
+          read($CTUnitNumber,'(A)',iostat=ios) phantomMediaRecord;
+          IF(ios ~= 0)[
+             OUTPUT61 i,j,k;
+             (/' ***ERROR reading CT phantom media row at ',3i6/);
+             STOP;
+          ]
+          IF(k=1 & j=1)[
+             integerMediumFormat = lnblnk1(phantomMediaRecord) > IMAX;
+             IF(integerMediumFormat)[
+                OUTPUT61;
+                (/' CT phantom media encoding: fixed-width I3 integers'/);
+             ]
+             ELSE[
+                OUTPUT61;
+                (/' CT phantom media encoding: EGSnrc base-62 characters'/);
+             ]
+          ]
+          ELSEIF((integerMediumFormat &
+                  lnblnk1(phantomMediaRecord) <= IMAX) |
+                 (~integerMediumFormat &
+                  lnblnk1(phantomMediaRecord) > IMAX))[
+             OUTPUT61 i,j,k;
+             (/' ***ERROR: mixed CT phantom media encodings at ',3i6/);
+             STOP;
+          ]
+          IF(integerMediumFormat)[
+             read(phantomMediaRecord,:integerMedFormat:,iostat=ios)
+                  (med($IR(i,j,k)),i=1,IMAX);
+          ]
+          ELSE[
+             read(phantomMediaRecord,:medformat:,iostat=ios)
+                  (medChar(i),i=1,IMAX);
+             IF(ios=0)[
+                DO i=1,IMAX[
+                   med($IR(i,j,k)) = index(encoding, medChar(i)) - 1;
+                ]
+             ]
+          ]
+          IF(ios ~= 0)[
+             OUTPUT61 i,j,k;
+             (/' ***ERROR decoding CT phantom media row at ',3i6/);
+             STOP;
+          ]
           DO i=1,IMAX[
-            med($IR(i,j,k)) = index(encoding, medChar(i)) - 1;
+             IF(med($IR(i,j,k)) < 0 | med($IR(i,j,k)) > NMED)[
+                OUTPUT61 med($IR(i,j,k)),i,j,k,NMED;
+                (/' ***ERROR: CT phantom medium ',i5,
+                  ' at voxel ',3i6,' is outside 0..',i5/);
+                STOP;
+             ]
           ]
        ]
        read($CTUnitNumber,*);
     ]
     :medformat: FORMAT($IMAXa1);
+    :integerMedFormat: FORMAT($IMAXi3);
 
     "input the densities"
     maxrhor=0.;
@@ -2612,8 +2667,9 @@ n_tot = ncaseold; first_time = .true.; is_finished = .false.;
 
 :start_parallel_loop:;
 
-IF(isource = 20 | isource = 21)["reset more_in_cont and MU index for every"
-	   "chunk"
+IF(isource = 20 | (isource = 21 & MLCflag ~= 2))[
+	   "reset legacy container state for every scheduler chunk; native"
+	   "source-21 products must drain across chunk boundaries"
 	   more_in_cont=0;
 	   frMU_indx=-1.0;
 ]
@@ -3476,6 +3532,9 @@ IF(isource=9 | isource=21 | (isource=20 &
 SHLflag=1 & MLCflag=0)) call finish_beamsource;
 
 IF((isource=20 | isource = 21) & MLCflag=1) [call finish_vcusource;]
+IF(isource=21 & MLCflag=2) [
+    call mcdose_particle_dmlc_dosxyz_finish_v1;
+]
 
 $CALL_EXIT(0);
 
diff --git a/dosxyznrc_user_macros.mortran b/dosxyznrc_user_macros.mortran
index 8430d98..3889a71 100644
--- a/dosxyznrc_user_macros.mortran
+++ b/dosxyznrc_user_macros.mortran
@@ -88,11 +88,11 @@
 "
 REPLACE {$STAT}    WITH {10}  "Number of batches to use in calcn of statistics"
                               "It works with $STAT = 1 but gives no stats"
-REPLACE {$MXMED}   WITH {20}   "Maximum number of media
+REPLACE {$MXMED}   WITH {100}  "Maximum number of media
 REPLACE {$MXSTACK} WITH {900000}  "Maximum particle stack size
-REPLACE {$IMAX}    WITH {128} "Maximum number of x cells
-REPLACE {$JMAX}    WITH {128} "Maximum number of y cells
-REPLACE {$KMAX}    WITH {128} "Maximum number of z cells
+REPLACE {$IMAX}    WITH {512} "Maximum number of x cells
+REPLACE {$JMAX}    WITH {512} "Maximum number of y cells
+REPLACE {$KMAX}    WITH {512} "Maximum number of z cells
 REPLACE {$MAXDOSE} WITH {{COMPUTE $IMAX*$JMAX*$KMAX+1}}
          "Number of dose regions, can be set to < $IMAX*$JMAX*$KMAX if "
          "necessary to reduce memory requirement, +1 for outside region"
diff --git a/srcxyznrc.macros b/srcxyznrc.macros
index 91a963c..f03d1f6 100644
--- a/srcxyznrc.macros
+++ b/srcxyznrc.macros
@@ -184,7 +184,10 @@ REPLACE {;COMIN/SOURCE/;} WITH {
              "2 => particles of any charge (for isource = 9 only)	       "
     SHLflag, "1 => send particle shared lib, 0 => no shared lib (isource=20)   "
     calflag, "0 => perform calibration run, 1 => no calib run (isource=20)     "
-    MLCflag, "1 => send particles through MLC, 0 => no MLC   (isource=20,21)   "
+    MLCflag, "MLC mode (source 20/21):"
+             "0 none, 1 legacy library, 2 native mcdose bridge"
+    i_source2_patient_frame,
+             "1 translation-only or 2 rotated patient-frame source-2 IAEA"
     ixinl,   "Index of lower x-boundary of beam                  (isource=0,6) "
     ixinu,   "Index of upper x-boundary of beam                  (isource=0,6) "
     jyinl,   "Index of lower y-boundary of beam                  (isource=0,6) "
@@ -246,7 +249,7 @@ REPLACE {;COMIN/SOURCE/;} WITH {
     ein, eksrcm, esrc_sp, SSD, NINCSRC,
     r_dbs, ssd_dbs,z_dbs,survival_ratio,einsrcold,iaea_SAD;
     $INTEGER isource, iqin, iqphsp, iqinc,latchold,iqinold,MLCflag,SHLflag,
-    calflag, ixinl, ixinu, jyinl, jyinu, kzinl, kzinu,
+    calflag, i_source2_patient_frame, ixinl, ixinu, jyinl, jyinu, kzinl, kzinu,
     klowx, khix, klowy, khiy, klowz, khiz,
     nsmiss, nsblocked, nsrjct,nsoutside, nmissm,enflag,
     dflag, ismode, IPRIMARY, OLDSRC,npassi,nang,ivary,ngang,numang,dose_stat,
diff --git a/srcxyznrc.mortran b/srcxyznrc.mortran
index 62ca146..7874caa 100644
--- a/srcxyznrc.mortran
+++ b/srcxyznrc.mortran
@@ -777,7 +777,7 @@ implicit none;
 "               read from first line.
 "
 "**************************************************************************"
-REPLACE {$NTEMP} WITH {12} "Dimension of temporary input buffer
+REPLACE {$NTEMP} WITH {13} "Dimension of temporary input buffer
 $REAL temp,fnorm,binsok,gridsz,ak;
 $INTEGER i,k,j,ib,ieof;
 dimension temp($NTEMP);
@@ -794,6 +794,7 @@ $REAL einm,xinm,yinm,uinm,vinm,wtm,zlastm,Z_SOURCE;
 enflag = 0; "Default flag: Source energy same for each source particle
 i_muidx_out = 0; "default not to output frMU_indx if writing phsp data"
 calflag = 0; "default to running the calibration run (isource = 20)"
+i_source2_patient_frame = 0; "default to standard source-plane IAEA semantics"
 
 OUTPUT; ( / / ' Source configuration'/);
 OUTPUT; (
@@ -1063,6 +1064,13 @@ ELSEIF(isource = 2)[ "Full phase space for each particle"
     phicol  = temp(7);     iqphsp=iqin;
     i_dbs=temp(8); r_dbs=temp(9); ssd_dbs=temp(10); z_dbs=temp(11);
     e_split=temp(12);
+    IF(temp(13) < 0 | temp(13) > 2)[
+      OUTPUT61 temp(13);
+      (//' ***ERROR in source 2:'/
+        ' patient-frame flag must be 0, 1, or 2; received ',f10.3//);
+      STOP;
+    ]
+    i_source2_patient_frame=temp(13);
     OUTPUT xiso,yiso,ziso,theta(1),phi(1),dsource,phicol,e_split;
     ( / ' Full phase space from an arbitrary direction'/
         ' x-coordinate of the isocenter:'    ,t55,f10.4/
@@ -1073,6 +1081,11 @@ ELSEIF(isource = 2)[ "Full phase space for each particle"
         ' Distance from isocenter to origin in source plane:',t55,f10.4/
         ' Source rotation angle:'            ,t55,f10.4/
         ' No. of times to split e+/e-:'    ,t55,i10/);
+    IF(i_source2_patient_frame>0)[
+      OUTPUT61 i_source2_patient_frame;
+      (/' IAEA coordinates: patient frame; transform mode ',i2/
+        ' Source-plane W and BEAM_SIZE rejection: disabled'/);
+    ]
     IF(i_dbs=1)[
       OUTPUT r_dbs,ssd_dbs,z_dbs;
       (/ ' Directional Bremsstrahlung Splitting (DBS) used'/
@@ -2274,6 +2287,10 @@ ELSEIF( (enflag = 2) | (enflag = 3) )["phase space input or full BEAM sim."
 	"no MLC
 	MLCflag = 0;
     ]
+    ELSEIF(the_vcu_code='mcdose_particle_dmlc')[
+        "native startup-artifact source bridge
+        MLCflag = 2;
+    ]
     ELSE[
 	"particles must go through MLC code compiled as a shared library
 	MLCflag = 1;
@@ -2428,13 +2445,13 @@ subroutine srcinit(weight);
 "
 "**************************************************************************"
 implicit none;
-;COMIN/GEOM,PHSPFILE,SOURCE,EGS-IO,RWPHSP/;
+;COMIN/GEOM,PHSPFILE,SOURCE,EGS-IO,RWPHSP,USEFUL/;
 
 $REAL weight,
       costheta,sintheta,
       coscol,sincol,
       cosphi,sinphi;
-$INTEGER i;
+$INTEGER i, status_mcdose, beam_number_mcdose, mlc_device_index_mcdose;
 
 OUTPUT61;(' ');
 
@@ -2755,12 +2772,25 @@ ELSEIF(isource = 21)["Full BEAM sim. incident from multiple settings and "
    survival_ratio = 1; "set to 1 to let vcu code know
 			"not to run calibration run or if not using MLC
 			"to set survival_ratio to 1
-   IF(MLCflag = 1)
-   [
+   IF(MLCflag = 1)[
 	OUTPUT 'About to initialise MLC code...';(//a);
 	call init_vcusource(survival_ratio,$CONFIGURATION_NAME,hen_house,
 			egs_home,the_vcu_code,the_vcu_input_file);
    ]
+   ELSEIF(MLCflag = 2)[
+        status_mcdose = -1;
+        call mcdose_particle_dmlc_dosxyz_init_v1(
+            prm, beam_number_mcdose, mlc_device_index_mcdose,
+            status_mcdose);
+        IF(status_mcdose ~= 0)[
+            OUTPUT61 status_mcdose;
+            (/' Native particleDmlc initialization failed with status ',I8/);
+            STOP;
+        ]
+        OUTPUT61 beam_number_mcdose,mlc_device_index_mcdose;
+        (/' Native particleDmlc initialized for beam ',I8,
+          ', DICOM MLC device index ',I8/);
+   ]
    ELSE[
 	OUTPUT61;
 	(/'No external library MLC'/);
@@ -3401,7 +3431,8 @@ $REAL RBEAM,DIST_RAY;
 $INTEGER ix,jy,kz,i,j,k,
          irin, "region of incident particle"
          latchi, "input latch value"
-         ibit;
+         ibit, begin_new_particle_mcdose,
+         status_mcdose;
 $LONG_INT n_hist_dum;
 
 save wsrc; "in case of phsp particle recycling"
@@ -3641,7 +3672,8 @@ ELSEIF(isource = 2 | isource=8)[ "Full phase space for each particle"
 
        nnread = nnread +1; "total actually read, initialized to 0 in main"
 
-       IF(wsrc < 0.0) [nsrjct=nsrjct+1; goto :READ-PH-SP-DATA:;]
+       IF(wsrc < 0.0 & i_source2_patient_frame=0)
+          [nsrjct=nsrjct+1; goto :READ-PH-SP-DATA:;]
        "particle is going backward"
 
        IF( npassi=1 ) [nsrjct=nsrjct+1; goto :READ-PH-SP-DATA:;]
@@ -3713,10 +3745,14 @@ ELSEIF(isource = 2 | isource=8)[ "Full phase space for each particle"
        ]"end of (enflag = 3) block"
 
        :FOUND-THE-PARTICLE:;
-       IF(ABS(xsrc) >= (BEAM_SIZE/2.0) | ABS(ysrc)>=(BEAM_SIZE/2.0))[
-          nsoutside=nsoutside+1;
+       IF(i_source2_patient_frame=0)[
+          IF(ABS(xsrc) >= (BEAM_SIZE/2.0) | ABS(ysrc)>=(BEAM_SIZE/2.0))[
+             nsoutside=nsoutside+1;
+          ]
        ]
-    ]UNTIL(ABS(xsrc) < (BEAM_SIZE/2.0) & ABS(ysrc)<(BEAM_SIZE/2.0));
+    ]UNTIL(i_source2_patient_frame>0 |
+           (ABS(xsrc) < (BEAM_SIZE/2.0) &
+            ABS(ysrc) < (BEAM_SIZE/2.0)));
     "end of LOOP"
     IF(NRCYCL>0)[
        CYCLNUM=1;
@@ -4060,7 +4096,7 @@ LOOP [;
 
 	]
 
-	IF(MLCflag=1)["if there is an MLC, pass particles through it
+	IF(MLCflag=1)["pass particles through the legacy MLC library
 		call sample_vcusource(einsrc,xsrc,ysrc,zsrcdum,usrc,
 			vsrc,wsrc,weight,iqin,latchi,n_hist_dum,
 			more_in_cont,frMU_indx);
@@ -4070,6 +4106,21 @@ LOOP [;
 				"more_in_cont=0;"
 			"]"
 	]
+        ELSEIF(MLCflag=2)["pass one product through the native source bridge
+            begin_new_particle_mcdose = 0;
+            IF(more_in_cont=0) begin_new_particle_mcdose = 1;
+            status_mcdose = -1;
+            call mcdose_particle_dmlc_dosxyz_sample_v1(
+                begin_new_particle_mcdose, iqin, einsrc,
+                xsrc, ysrc, zsrcdum, usrc, vsrc, wsrc,
+                weight, latchi, frMU_indx, more_in_cont,
+                status_mcdose);
+            IF(status_mcdose ~= 0)[
+                OUTPUT61 status_mcdose;
+                (/' Native particleDmlc sampling failed with status ',I8/);
+                STOP;
+            ]
+        ]
 
 
         IF( iqinc < 2 & iqin ~= iqinc ) [
@@ -4155,7 +4206,25 @@ IF(NofREPEAT > 0 & ISMOOTH = 1)[
 ]
 
 "Rotate/translate the source particle into position
-IF(isource<7 | isource=9)[
+IF(isource=2 & i_source2_patient_frame=1)[
+  "Translation-only patient frame exactly preserves stored axes."
+  uin = usrc;
+  vin = vsrc;
+  win = wsrc;
+  xsrcp = xsrc + xiso;
+  ysrcp = ysrc + yiso;
+  zsrcp = zsrc + ziso;
+]
+ELSEIF(isource=2 & i_source2_patient_frame=2)[
+  "Rotated patient frame retains stored Z and both signs of W."
+  uin   = r_11(1)*usrc + r_12(1)*vsrc + r_13(1)*wsrc;
+  vin   = r_21(1)*usrc + r_22(1)*vsrc + r_23(1)*wsrc;
+  win   = r_31(1)*usrc + r_32(1)*vsrc + r_33(1)*wsrc;
+  xsrcp = r_11(1)*xsrc + r_12(1)*ysrc + r_13(1)*zsrc + xiso;
+  ysrcp = r_21(1)*xsrc + r_22(1)*ysrc + r_23(1)*zsrc + yiso;
+  zsrcp = r_31(1)*xsrc + r_32(1)*ysrc + r_33(1)*zsrc + ziso;
+]
+ELSEIF(isource<7 | isource=9)[
   IF(i_iaea_in=1) zsrc = zsrc - iaea_SAD;
 
   uin   = r_11(1)*usrc + r_12(1)*vsrc + r_13(1)*wsrc;
